[package]
name = "paylink"
version = "0.1.0"
description = "Solana payment link escrow protocol — send USDC/USDT via shareable URLs"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "paylink"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.29.0"
anchor-spl = "0.29.0"
